# Dev Setup

First intall pipenv

```
pip3 install pipenv
```

Second clone the repo and install dependencies:

```
pipenv install
```

Next, activate the virtual environment:

```
pipenv shell
```

Run the main app:
```
python main.py
```


